name: Capture Studio host tests

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: read

jobs:
  tests:
    if: >-
      github.actor != 'github-actions[bot]' &&
      github.event.pull_request.head.ref == 'agent/extract-studio-host'
    runs-on: ubuntu-latest
    steps:
      - name: Check out Studio branch
        uses: actions/checkout@v4
        with:
          ref: agent/extract-studio-host

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.13'

      - name: Capture test output
        shell: bash
        run: |
          set -euo pipefail
          bun install
          bun test > studio-host-tests.log 2>&1 || true

      - name: Upload test output
        uses: actions/upload-artifact@v4
        with:
          name: studio-host-tests
          path: studio-host-tests.log
          if-no-files-found: error
          retention-days: 1

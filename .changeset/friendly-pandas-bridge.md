---
'@ankhorage/studio': patch
---

Prepare generated Android development builds for local Supabase by recovering the complete application runtime through the Studio host, then supervising ADB transport changes while Expo runs so every replacement device transport receives a verified reverse mapping.

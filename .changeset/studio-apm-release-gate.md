---
'@ankhorage/studio': patch
---

Gate Studio owner releases using the published Devtools packed-artifact validator. Fix native Node loading of the headless APM extension, reject malformed package plans and mismatched target artifacts, serialize reviewed package writes, and share one pure package policy across generation and projection.

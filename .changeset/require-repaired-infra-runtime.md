---
'@ankhorage/studio': patch
---

Require `@ankhorage/infra` 4.1.1 and keep Studio's runtime recovery boundary topology-agnostic so generated Minikube forwards derive from configured deploy targets and survive profile restarts without targeting absent services.

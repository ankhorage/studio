---
'@ankhorage/studio': patch
---

Require `@ankhorage/infra` 4.1.1 so generated Minikube runtime recovery derives forwards from configured deploy targets and survives profile restarts without targeting absent services.

---
"@ankhorage/studio": patch
---

Fail closed at the Studio host restart boundary whenever the selected Studio owner version differs from the running host, even if separate host-update availability evidence is missing.

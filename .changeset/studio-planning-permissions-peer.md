---
'@ankhorage/studio': patch
---

Declare the Permissions package required by the public Expo Runtime planning contract so
standalone Registry consumers can load the Studio host without a workspace-masked
transitive dependency.

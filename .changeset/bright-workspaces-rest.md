---
'@ankhorage/studio': patch
---

Align the Studio app with the expected Expo SDK 54 patch versions and restrict the root workspace to the first-party `apps/studio` app so locally generated projects are not treated as repository workspaces.
